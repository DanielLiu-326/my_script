mod script_types;

use crate::script_types::Reference;
use std::collections::{HashMap, VecDeque};

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
